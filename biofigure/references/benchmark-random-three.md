# 随机三例：新代码迁移测试

2026-09-14，以种子20260915从114个未运行候选抽样。查看对应原图和代码后，用模拟数据写新R脚本；无生成隔离，不作为盲测或全量能力认证。

- scidraw-af521313e0：circlize环柱。每扇区单独计算均值＋SEM上界与标尺；柱中心、误差线和显著性字母在同一循环计算。先画外环和名称轨，再画主数据轨。Tukey比较生成字母，禁止沿用源码循环外的max_value/xleft。不同单位不可按半径横向比较。
- scidraw-a54e1600ef：双条件logFC。按唯一gene_id连接，按两个条件的FDR分别选方向极值再取并集；异向另编码。ggrepel必须看最终图，标签避让不等于不盖住未标注点；密集时按y排序、给两侧标签槽位及有界初始位置，避免长引线穿越点群。尺度改变需重新验图。
- scidraw-c607bd2c3e：分组散点。类别顺序和四组位置固定，颜色区分条件、空心实心区分处理；ggpubr接收已计算检验表。配对检验必须有实际配对ID，本例按独立模拟鼠做Welch检验并在八个预设比较内BH校正。

三图保留主要参考结构。logFC经历标签修正。统计只属于模拟输入，来源数值和字体不照搬。后续仍需留出数据与规模/长标签测试；本记录不能自动升级benchmark PASS。

## 第二次随机三例

种子20260916，从111个未运行候选抽取β森林、位点突变热图、三面板时序。查看来源代码和图片，以模拟输入重新编码；仅作来源知情预览，不是盲测。

- 森林：ggplot2分配表格与CI槽位；越界CI用箭头；q<0.001显示阈值，禁止四舍五入为0。估计值为模拟量，不声称实测回归。
- 突变：20类氨基酸×位点矩阵；列均值与右轴用可逆映射；分类格不插值。使用固定坐标比例保留正方格。
- 时序：ggplot2+patchwork，共用尺度；SEM按同一主体内10个模拟trial计算，不能充当动物间推断；保留刺激箭头。

三例尚缺完整CP0–CP4及独立迁移/压力证据。出图不升级认证。
